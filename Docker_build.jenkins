#!groovy
properties([disableConcurrentBuilds()])
pipeline {
	agent {
    label ''master
  }
  stages {
  	stage('create docker images') {
      steps {
        echo "===================== start building images ===================="
      	dir ('Dockerfile') {
          sh 'docker build .'
        }
      }
    }
  }
}
